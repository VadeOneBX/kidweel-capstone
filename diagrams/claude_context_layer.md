# Claude Context Layer - System Placement

```
           Candidate Ingestion
   (SQUEEZE / VRP / REVERSE_VRP)
                    |
                    v
        Deterministic Screening
   (signal alignment, tradeability)
                    |
                    v
          Constraint Layer
   (playbook + RR + PMP + DTE)
                    |
                    v
        Structure Construction
                    |
                    v
            Risk Evaluation
                    |
                    v
            Backtest Engine
                    |
                    v
             Evidence Output
                    |
                    v
        Claude Context Layer
   (reliability assessment only)
                    |
                    v
         Research Comparison
```

---

## Notes

- Claude is NOT in the execution path
- Claude does NOT modify decisions
- Claude evaluates context degradation only

---

## Principle

Constraints define what is allowed.

Claude identifies when those constraints break.
